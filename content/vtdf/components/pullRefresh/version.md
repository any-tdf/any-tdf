## 3.0.0

- [!tag|A|0|] 新增 PullRefresh 组件，支持受控刷新状态、成功提示、自定义状态内容和自定义滚动容器。
- [!tag|A|0|] 新增 `maxDistance` 属性，超过 `threshold` 的下拉距离按阻尼曲线衰减。
- [!tag|O|0|] 跟手阶段禁用过渡动画，下拉更跟手；新增手势方向锁，回拖超过起点可取消下拉。
- [!tag|O|0|] 支持桌面端鼠标按住拖拽刷新，头部状态区域增加 `aria-live` 播报。
