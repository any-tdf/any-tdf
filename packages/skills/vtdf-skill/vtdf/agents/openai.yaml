interface:
  display_name: 'VTDF'
  short_description: 'Build and review VTDF Vue 3 mobile UI projects.'
  default_prompt: 'Use $vtdf to build a VTDF feature with exact component APIs and the matching project conventions.'
